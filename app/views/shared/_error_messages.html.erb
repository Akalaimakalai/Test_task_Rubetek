<% if resource.present? && resource.errors.any? %>
  <%= content_tag :div, data: { resource_id: resource.id }, id: "error_explanation" do %>
    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  <% end %>
<% end %>
